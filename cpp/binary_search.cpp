#include <iostream>
#define max_len 100
using namespace std

void search(int a[], int);

int main(){
	int a[max_len];
    cout << "Provide less than " << max_len << " integers which are separated by whitespace: " << endl;

	return 0;
}